export let time = {
  DT: '%Y-%m-%d %H:%M',
  DTS: '%Y-%m-%d %H:%M:%S',
  TS: '%H:%M:%S',
  T: '%H:%M',
  D: '%Y-%m-%d',
  str2Date (s, format = this.DT) {

  },
  date2Str (d, format = this.DT) {

  },
  toTimeStamp (s, format = this.DT, n = 13) {

  },
  fromTimeStamp (t, format = this.DT, n = 13) {

  },
  transFormat (f1, f2) {

  },
  now (format = this.DT) {

  },

  // date is Date() or string
  addYear (date, year, format = this.DT) {

  },

  // date is Date() or string
  addMonth (date, month, format = this.DT) {

  },
  // date is Date() or string
  addDay (date, day, format = this.DT) {

  },
  // date is Date() or string
  addHour (date, hour, format = this.DT) {

  },
  // date is Date() or string
  addMinute (date, minute, format = this.DT) {

  },
  // date is Date() or string
  addSecond (date, second, format = this.DT) {

  },
  addDateStr (s1, s2, f1, f2) {

  },
}
